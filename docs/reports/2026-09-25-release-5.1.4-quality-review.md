# Revisión final de release 5.1.4

Issue: #629 — release: publish oxidize-pdf 5.1.4 maintenance fixes — https://github.com/bzsanti/oxidizePdf/issues/629

## Resumen ejecutivo

Revisión del delta de preparación sobre develop `410358d8b199c15910e7434e6be2ec7d278a03c7`:
Cargo.toml, Cargo.lock, README.md, CHANGELOG.md y TASKS.md. Se contrastaron además
los diffs del parser, extracción y streaming y las revisiones/validaciones
integradas de #615–#619 y #620. Estas revisiones previas conservan sus límites y
hallazgos históricos; esta promoción no añade cambios de implementación.
Rust 2021, MSRV 1.88; biblioteca síncrona con features opcionales de firmas.
El grafo del lockfile es idéntico al de develop salvo la versión del paquete.

Versiones 5.1.4 consistentes en workspace, lockfile y ejemplos de instalación.
El changelog enumera las correcciones y no atribuye la eliminación de C a esta
release. La publicación antes de #627 está explícitamente autorizada; #627 sigue
pendiente y obligatoria. No se alteran matrices ISO ni claims de conformidad.

## Hallazgos

1. **Versiones retiradas ya fijadas en el lockfile**: `Cargo.lock:357` y `Cargo.lock:2367` (entradas
   chacha20 0.10.0 y spin 0.9.8) — el empaquetado emite avisos del registro.
   El grafo no cambia respecto de develop; no se deduce vulnerabilidad ni fallo
   de publicación de estos avisos. Investigar el motivo y actualizar con
   validación bajo una issue específica. Seguimiento bloqueado en TASKS.md por
   falta de issue; responsable de crearla: bzsanti. No se corrige en la release.

Los hallazgos preexistentes del buffer por lotes y del test temporal de memoria
siguen registrados en el seguimiento anterior. No se presentan como resueltos.
No se encontró un defecto nuevo en el delta de versión/documentación revisado.

## Calidad de Tests (Kripteia)

Ejecución real sobre oxidize-pdf-core: **94/100, 9.797 tests, 695 archivos**.
Los tests con menor puntuación automática (40) son
`test_custom_font_loading_from_bytes`, `test_custom_font_with_text`,
`test_deeply_nested_arrays` y `test_deeply_nested_dictionaries`; son anteriores
al delta de release. No se interpreta el score automático como cobertura.
La ejecución Python reporta **100, 0 tests, 0 archivos**: no reconoce unittest,
por lo que ese score no aporta evidencia de calidad. Sus **33 tests** se
validaron ejecutándolos; el exportador Rust pasa **6 tests**.

Las regresiones de #609 comprueban bytes/texto para LF/CR/CRLF; #615 protege
límites posicionados y puntuación; #616 exige FOOTER tras round trip; #617
conserva texto sano tras errores; #618 comprueba cancelación y asignaciones
antes del callback con una cola ampliada. Retirar los comportamientos corregidos
debe romper esos oráculos; las mutaciones de la implementación están documentadas
en sus informes anteriores, no se atribuyen a esta sesión.

## Análisis de Seguridad (Kripteia Security)

Salida Rust: tres usos unsafe, `alloc`, `dealloc` y `realloc` en
`oxidize-pdf-core/tests/issue_618_incremental_callbacks_test.rs:23`, `:27`, `:30`.
Inspección: delegan puntero, tamaño y layout a System, con contadores por hilo
que no asignan memoria; código de pruebas ya revisado. Ningún otro hallazgo del
escáner. Salida Python: **No security issues found**.
El delta de release no incorpora dependencias, unsafe, ejecución ni entradas
nuevas. La cadena hacia ring permanece bajo #627.

## Validación

- `cargo test --locked --offline --workspace`: 9.801 pasan, 0 fallan, 73 ignorados;
  incluye 6.791 tests de biblioteca y 217 doctests.
- `cargo clippy --locked --offline --workspace --all-targets -- -D warnings`: pasa.
- `cargo fmt --all --check` y `git diff --check`: pasan.
- Contrato Python 33/33 y exportador Rust 6/6: pasan.
- `cargo package --locked --offline -p oxidize-pdf --allow-dirty`: pasa,
  incluido el build del paquete extraído (995 archivos, 19 MiB / 5,2 MiB comprimido).
- Corpus externo y OmniDocBench oficial: se conserva la evidencia integrada
  del 24/09, incluido el flujo oficial de #620 y las 981 predicciones. No se
  afirma haber repetido ese corpus en esta copia aislada ni se recalibran métricas.
- CI de #628: checks aplicables verdes antes de su merge. La CI de promoción
  a main se comprobará por separado antes de fusionar y etiquetar.

## Métricas

- Archivos del delta de preparación revisados: 5.
- Hallazgos nuevos del empaquetado: 1 (dependencias preexistentes).
- Archivos de implementación modificados: 0.
- Logs: `/tmp/oxidize-5.1.4-{tests,clippy,contract,exporter,package,kripteia-rust,kripteia-python}.log`.
